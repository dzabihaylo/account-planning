---
phase: 08-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - UIPOL-01
must_haves:
  truths:
    - "Switching between any two tabs shows consistent card padding, font sizes, and color usage"
    - "No 12px font-size values remain in labels — all are 11px"
    - "All .card elements have 16px padding on all sides"
    - "Shared CSS classes .section-header, .gd-toast, .sidebar-rename-input exist for downstream plans"
  artifacts:
    - path: "index.html"
      provides: "Normalized CSS with shared classes and consistent typography"
      contains: ".section-header"
  key_links:
    - from: ".section-header class"
      to: "Contacts tab section headers"
      via: "class attribute replacement"
      pattern: "section-header"
---

<objective>
Normalize visual inconsistencies across all 6 tabs and define shared CSS classes that downstream plans will use.

Purpose: Establish consistent spacing, typography, and card styles (UIPOL-01), and create the CSS classes (.section-header, .sidebar-rename-input, .gd-toast) that Plans 02 and 03 depend on.
Output: Updated index.html with normalized CSS and shared classes ready for use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-polish/08-CONTEXT.md
@.planning/phases/08-ui-polish/08-RESEARCH.md
@.planning/phases/08-ui-polish/08-UI-SPEC.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared CSS classes and normalize .card padding</name>
  <files>index.html</files>
  <read_first>index.html (lines 1-700 for all CSS)</read_first>
  <action>
In the CSS section of index.html, make these changes:

1. **Normalize .card padding** (per D-17, UI-SPEC line 113): Change `.card` padding from `18px 20px` to `16px`. This is a single rule change.

2. **Add .section-header class** (per D-15, UI-SPEC line 117-125): Add new shared class:
```css
.section-header {
  font-size: 11px;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--muted);
  margin-bottom: 8px;
}
```

3. **Add .sidebar-rename-input class** (per D-15, UI-SPEC line 160-168): Add new class for Plan 03's inline rename:
```css
.sidebar-rename-input {
  background: transparent;
  border: none;
  border-bottom: 1px solid var(--accent);
  color: var(--text);
  font-size: 11px;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  font-family: var(--font);
  outline: none;
  width: 100%;
}
```

4. **Add .gd-toast classes** (per D-15, UI-SPEC line 179-183): Add toast CSS for Plan 03's error feedback:
```css
.gd-toast {
  position: fixed;
  bottom: 24px;
  right: 24px;
  z-index: 2000;
  background: var(--card);
  border: 1px solid var(--border2);
  border-radius: 8px;
  padding: 8px 16px;
  font-size: 13px;
  color: var(--text);
  animation: fi 0.15s ease;
  transition: opacity 0.15s;
}
.gd-toast-error { border-left: 3px solid var(--red); }
.gd-toast-success { border-left: 3px solid var(--green); }
```

5. **Normalize .stat-val font-size**: Change from `22px` to `20px` (per UI-SPEC line 221 — display role normalization).

6. **Normalize 12px label font-sizes to 11px** (per D-17, UI-SPEC line 222): Find all occurrences of `font-size: 12px` in label-role elements and change to `11px`. Specifically target:
   - `.bar-row` font-size
   - `.topnav-count` font-size
   - `.activity-meta` font-size (line ~561)
   - `.activity-form-error` font-size (line ~501)

7. **Normalize 10px to 11px** for these small-label elements:
   - `.sidebar-revenue` (line ~107) — 10px to 11px
   - `.sig-meta` (line ~214) — 10px to 11px
   - `.exec-loc` (line ~243) — 10px to 11px

Do NOT change:
- `.sidebar-section` font-size (10px) — this is a non-interactive label row, acceptable per UI-SPEC
- `.briefing-section-header` at 15px — this is a document heading role per UI-SPEC line 220
- `.card` border-radius — retain existing 10px

IMPORTANT: The `.card` padding change affects all tabs simultaneously. This is intentional per UI-SPEC.
  </action>
  <verify>
    <automated>grep -c "font-size: 12px" index.html | xargs test 0 -eq && echo "PASS: no 12px font-sizes" || echo "FAIL: 12px font-sizes remain"</automated>
  </verify>
  <acceptance_criteria>
    - grep "\.section-header" index.html returns the new class definition
    - grep "\.sidebar-rename-input" index.html returns the new class definition
    - grep "\.gd-toast" index.html returns the new class definitions
    - grep "font-size: 12px" index.html returns zero matches
    - grep "padding: 16px" index.html shows .card padding normalized
    - grep "font-size: 20px" index.html shows .stat-val normalized (was 22px)
  </acceptance_criteria>
  <done>All shared CSS classes defined. Card padding normalized to 16px. All label-role 12px values changed to 11px. All 10px small-label values changed to 11px. .stat-val at 20px.</done>
</task>

<task type="auto">
  <name>Task 2: Apply shared classes to tab content and normalize spacing</name>
  <files>index.html</files>
  <read_first>index.html (lines 700-1100 for tab renderer HTML; search for renderContactsTab, renderActivityTab, renderStrategyTab to find JS renderers)</read_first>
  <action>
Apply the shared CSS classes created in Task 1 to existing tab content. These are changes in the JavaScript rendering functions.

1. **Contacts tab section headers** (per UI-SPEC line 217): In the contacts tab renderer function, find section headings that use ad-hoc inline styles (like inline font-size, text-transform, etc.) and replace with `class="section-header"`. The existing heading elements should get the shared class instead of inline style overrides.

2. **Activity tab entry spacing** (per UI-SPEC line 218): In the activity tab renderer, find the container that holds activity entries and ensure it uses `gap: 8px` for consistent spacing. If entries use inconsistent `margin-bottom`, normalize by adding a CSS rule: `.activity-timeline { display: flex; flex-direction: column; gap: 8px; }` (add this rule in the CSS section). Remove any conflicting `margin-bottom` on individual activity entries if present.

3. **Strategy tab inline form inputs** (per UI-SPEC line 219): Audit the strategy tab renderer. Ensure any inline form inputs use the `.form-input` class with the standard styles (padding: 8px 12px, font-size: 13px, focus border-color: var(--accent)). If strategy-specific input classes override these, normalize them.

4. **Modal research status font-size**: In the modal HTML at line 983, change the `font-size:12px` in the research status div to `font-size:11px` (this is a label-role text).

Do NOT modify:
- The Briefing tab `.briefing-section-header` at 15px (document heading role, explicitly retained per UI-SPEC)
- Any functional JavaScript logic — only class attributes and CSS rules

After changes, visually verify by searching for any remaining inline `font-size` overrides in renderer functions that contradict the typography scale (11px labels, 13px body, 15px headings, 20px display).
  </action>
  <verify>
    <automated>grep -c "section-header" index.html | xargs test 1 -le && echo "PASS: section-header class used" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "section-header" index.html returns matches in both CSS definition and tab renderer usage
    - grep "activity-timeline" index.html shows gap: 8px rule (if activity container class exists)
    - The modal research status text uses 11px not 12px
  </acceptance_criteria>
  <done>Shared classes applied across all relevant tabs. Contacts tab uses .section-header. Activity tab has consistent 8px entry gaps. Strategy tab inputs follow .form-input spec. All typography follows the 11/13/15/20 scale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan is CSS-only changes — no user input, no API calls, no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | CSS class names | accept | CSS changes are cosmetic only; no security impact from spacing/typography normalization |
</threat_model>

<verification>
1. Load the app in browser, navigate to each of the 6 tabs (Overview, Contacts, Activity, Strategy, Briefing, Ask AI) for any account
2. Confirm card padding is visually consistent across all tabs
3. Confirm no text appears at 12px that should be 11px
4. Run: `grep -c "font-size: 12px" index.html` — must return 0
5. Run: `grep "\.section-header" index.html` — must show the class definition
6. Run: `grep "\.gd-toast" index.html` — must show the class definition
</verification>

<success_criteria>
- All .card elements render with 16px padding
- No 12px font-size values remain in label-role CSS
- Shared CSS classes (.section-header, .sidebar-rename-input, .gd-toast) are defined and available
- Visual consistency is maintained across all 6 tabs
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-polish/08-01-SUMMARY.md`
</output>
