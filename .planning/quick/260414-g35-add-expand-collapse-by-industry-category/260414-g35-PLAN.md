---
phase: quick
plan: 260414-g35
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
must_haves:
  truths:
    - "Each industry category header is clickable and toggles its group of accounts"
    - "A chevron indicator shows expand/collapse state on each header"
    - "All sections start expanded by default so no accounts are hidden on load"
    - "Search/filter still works correctly — matching accounts are visible regardless of collapse state"
    - "Active account's section is always expanded"
  artifacts:
    - path: "index.html"
      provides: "Collapsible sidebar sections with CSS and JS"
  key_links:
    - from: "renderSidebar()"
      to: "sidebar-section click handler"
      via: "onclick toggleSection()"
    - from: "filterAccounts()"
      to: "sidebar-group visibility"
      via: "shows all groups during search, restores collapse state when cleared"
---

<objective>
Add expand/collapse behavior to the sidebar industry category headers (Automotive OEM, Automotive Tier 1, Financial Services, Energy & Utilities). Each section header becomes clickable with a chevron indicator. Clicking toggles visibility of that section's account list. All sections start expanded. Search overrides collapse state to show matches.

Purpose: Improve sidebar navigation as the account list grows — lets users collapse categories they are not actively working with.
Output: Updated index.html with collapsible sidebar sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS for collapsible sidebar sections</name>
  <files>index.html</files>
  <action>
Add CSS rules after the existing `.sidebar-section` style (around line 83) for the collapsible behavior:

1. Make `.sidebar-section` a clickable flex row with `cursor: pointer; display: flex; align-items: center; justify-content: space-between;` and add a hover state that brightens the text slightly.

2. Add a `.sidebar-section-chevron` style: a small chevron indicator (use a CSS border-based triangle or Unicode character like the existing codebase style). The chevron should rotate when collapsed — use a CSS transition (`transform: rotate(-90deg)` for collapsed state, `rotate(0deg)` for expanded).

3. Add a `.sidebar-group` class for the wrapper div around each section's account buttons. Default state: visible. Add `.sidebar-group.collapsed` that sets `display: none`.

Keep styles consistent with existing design language — use `var(--muted)` for the chevron color, keep the existing uppercase/letter-spacing on section text.
  </action>
  <verify>
    <automated>grep -c "sidebar-group" index.html | grep -q "[1-9]" && grep -c "sidebar-section-chevron" index.html | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CSS classes for sidebar-group, sidebar-section-chevron, and collapsed state exist in the style block.</done>
</task>

<task type="auto">
  <name>Task 2: Update renderSidebar() and add toggleSection(), update filterAccounts()</name>
  <files>index.html</files>
  <action>
Modify the `renderSidebar()` function (starting around line 1111):

1. In the sector loop, change the `.sidebar-section` div to include an onclick handler and a chevron span:
   ```
   html += '<div class="sidebar-section" onclick="toggleSection(\'' + escapeHtml(sector) + '\')">';
   html += '<span>' + escapeHtml(sector) + '</span>';
   html += '<span class="sidebar-section-chevron" id="chev-' + sectorIndex + '">&#9660;</span>';
   html += '</div>';
   ```
   Use a simple index (loop variable `i`) for the chevron ID.

2. Wrap each section's account buttons in a `<div class="sidebar-group" id="group-{i}">` ... `</div>` so they can be toggled as a unit.

3. Add a `toggleSection(sector)` function:
   - Find the `.sidebar-group` that follows the clicked `.sidebar-section` (use the same index approach, or traverse DOM — simplest: use data attributes or IDs like `group-0`, `group-1`, etc., with the section header storing `onclick="toggleSection(0)"`)
   - Toggle the `collapsed` class on the group div
   - Rotate the chevron (toggle a `collapsed` class on the chevron or its parent)
   - Store collapsed state in a module-level object `var sectionState = {}` keyed by sector name so state persists across re-renders

4. Update `filterAccounts()` (around line 1416):
   - When a search query is active (q is non-empty): show ALL `.sidebar-group` divs (remove `collapsed` class) and also show/hide `.sidebar-section` headers based on whether any child item matches
   - When search is cleared (q is empty): restore collapsed state from `sectionState` object
   - Continue showing/hiding individual `.sidebar-item` elements as before

5. Ensure `showAccount()` expands the section containing the selected account if it is collapsed — so clicking an account from any navigation path always reveals it.

Use the same coding style as the rest of the file: var declarations, string concatenation with +, no template literals in functions that use older style, inline onclick handlers.
  </action>
  <verify>
    <automated>grep -c "toggleSection" index.html | grep -q "[1-9]" && grep -c "sidebar-group" index.html | grep -q "[2-9]" && grep -c "sectionState" index.html | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- Sidebar renders with clickable section headers and chevron indicators
- Clicking a section header collapses/expands that group of accounts
- All sections start expanded
- Search overrides collapse state to show matching items, restores state when cleared
- Selecting an account expands its parent section if collapsed
  </done>
</task>

</tasks>

<verification>
1. Open the app locally (`node server.js`) and verify all 4 industry categories show with chevron indicators
2. Click each category header — accounts underneath should hide/show with chevron rotating
3. Collapse a section, then use search to find an account in that section — it should appear
4. Clear search — previously collapsed section should return to collapsed state
5. All accounts remain accessible and clickable
</verification>

<success_criteria>
- All 4 industry category headers are clickable with visual chevron indicator
- Toggle behavior works: click to collapse, click again to expand
- Sections start expanded by default
- Search/filter correctly overrides and restores collapse state
- No visual regressions to sidebar styling
</success_criteria>

<output>
After completion, create `.planning/quick/260414-g35-add-expand-collapse-by-industry-category/260414-g35-SUMMARY.md`
</output>
